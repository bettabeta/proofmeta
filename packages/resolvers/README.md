# Reference resolvers

Plug-in services that fulfill **payment**, **delivery**, or **verification** steps. ProofMeta defines how agents name and select them; implementation lives here.

v1 target: one **free/open** reference resolver (no on-chain scope per `PROOFMETA_ANWEISUNG.md`).
