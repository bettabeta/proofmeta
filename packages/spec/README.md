# ProofMeta JSON Schemas

Canonical machine-readable specs live here.

| File | Role |
|------|------|
| `manifest.schema.json` | Provider manifest (`/.well-known/proofmeta.json`) |
| `request.schema.json` | License request / in-flight document |
| `status.schema.json` | Status GET response (draft; may converge with `request.schema.json`) |

Human-readable normative text: [`../../PROOFMETA_ANWEISUNG.md`](../../PROOFMETA_ANWEISUNG.md) and [`../../docs/`](../../docs/).
