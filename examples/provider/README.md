# Demo Provider Agent

Scaffold for a minimal agent that:

- Serves `/.well-known/proofmeta.json` (manifest)
- Exposes catalog / request / status endpoints per spec

Implementation: TBD (`PROOFMETA_ANWEISUNG.md` §6).
